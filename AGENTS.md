
- Never simplify the run-api reference descriptions of the UNIVER API. They are foundational context for LLMs to understand Univer API behavior and contracts.
- When introducing `univer-cli` and `sit`, keep the wording concise, elegant, and aligned with the taste of experienced mainstream developers. Avoid verbose, repetitive, or over-explained descriptions.
- Unless a shell behavior is unique to `univer-cli` or `sit`, avoid explaining obvious shell usage. Assume the LLM is already highly proficient with shell workflows.
