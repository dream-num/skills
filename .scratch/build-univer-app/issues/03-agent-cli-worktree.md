# 03 — Add Agent/CLI editing through Worktree

**What to build:** Make the skill connect a CLI headless runtime to the same authoritative collaboration data as browser clients and guide a safe Worktree-based Agent edit, review, merge, or discard workflow.

**Blocked by:** 02 — Guide and build a self-hosted Workspace.

**Status:** complete

- [x] Guidance covers target resolution, authentication, pull, execute, commit, retry, terminal conflict, and Worktree review semantics.
- [x] The skill prevents automatic browser collaboration synchronization from competing with the CLI manual runtime state machine.
- [x] English and Chinese developer guidance explains the shared human and Agent data flow.
- [x] A clean-context Agent produces a correct Agent/CLI and Worktree integration plan with executable validation steps.
