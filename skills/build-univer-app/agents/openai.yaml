interface:
  display_name: "Build Univer App"
  short_description: "Route and design cross-SDK Univer apps"
  default_prompt: "Use $build-univer-app to define this Univer application's architecture and route implementation to the owning SDK Skills."
